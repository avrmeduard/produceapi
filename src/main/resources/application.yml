spring:
  output:
    ansi:
      enabled: ALWAYS
server:
  port: 8081
logging:
  file: logs/mvc_pattern2.log
  pattern:
    console: "%d{yyyy-MMM-dd HH:mm:ss.SSS} [%thread] %highlight(%-5level) %cyan(%logger{15}) - %msg %n"
    file: "%d %-5level [%thread] %logger : %msg%n"
  level:
    ROOT: INFO
